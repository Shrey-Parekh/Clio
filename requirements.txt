# Dependencies are added as each phase needs them, not speculatively.

# Phase 0.3 - config
python-dotenv==1.2.3

# Phase 1.2 - TTS engine (Kokoro, local/offline)
kokoro-onnx==0.6.1
sounddevice==0.5.6

# Phase 1.3 - audio input, VAD (Silero, raw ONNX - no torch)
numpy==2.4.6
onnxruntime-gpu==1.29.0

# Phase 1.4 - STT: local faster-whisper (CUDA) + Groq as an alternate provider
faster-whisper==1.2.1
groq==1.7.0
soundfile==0.14.0
# CTranslate2's CUDA backend needs these at runtime; not auto-discovered on
# Windows without the PATH fix in clio/speech/stt.py - see _ensure_cuda_dlls_on_path.
nvidia-cublas-cu12==12.9.2.10
nvidia-cudnn-cu12==9.25.1.1

# Phase 1.5 - wake word (openWakeWord, raw ONNX runtime - training happens in WSL,
# see docs/wake_word_training.md; this is only the Windows runtime dependency)
openwakeword==0.6.0

# Phase 1.7 - tool calling schema validation
jsonschema==4.26.0
