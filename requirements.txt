# Dependencies are added as each phase needs them, not speculatively.

# Phase 0.3 - config
python-dotenv==1.2.3

# Phase 1.2 - TTS engine (Kokoro, local/offline)
kokoro-onnx==0.6.1
sounddevice==0.5.6

# Phase 1.3 - audio input, VAD (Silero, raw ONNX - no torch)
numpy==2.4.6
onnxruntime==1.29.0
