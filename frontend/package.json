{
  "name": "clio-frontend",
  "version": "0.1.0",
  "private": true,
  "description": "Clio's Tauri shell - a window onto the core over WebSocket.",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
