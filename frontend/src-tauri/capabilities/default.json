{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the HUD and the panel, plus autostart.",
  "windows": ["main", "panel"],
  "permissions": ["core:default", "autostart:default"]
}
